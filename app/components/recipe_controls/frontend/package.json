{
  "name": "recipe-controls-component",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build": "tsc && vite build",
    "watch": "vite build --watch"
  },
  "dependencies": {
    "@streamlit/component-v2-lib": "^0.2.0"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "vite": "^5.4.0"
  }
}
